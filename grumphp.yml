grumphp:
    fixer:
        enabled: true
        fix_by_default: true
    tasks:
        phpcs:
            standard:
                - PSR12
            triggered_by: [php]
            ignore_patterns:
                - tests/
#        shell:
#            scripts:
#                - [ "-c", "./vendor/bin/phpstan analyse" ]
